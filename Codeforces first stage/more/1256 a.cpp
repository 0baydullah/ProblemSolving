#include<bits/stdc++.h>
#define fuckl "\n"
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0); cin.tie(NULL); cout.tie(NULL);

        int t;
        cin >> t;
        while(t--)
        {
            int a,b,n,s;
            cin >> a >> b >> n >> s;

            if(a*n+b < s) cout << "no" << fuckl;
            else if(a*n+b == s) cout << "yes" << fuckl;
            else
            {
                if(s%n != 0 && )
            }
        }

    return 0;
}
