#include<bits/stdc++.h>
#define fuckl "\n"
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0); cin.tie(NULL); cout.tie(NULL);

    int cnt=0,t;
    cin >> t;
    int i,j,k;
    while(t--)
    {
        string a;
        cin >> a;
        for(i=0;i<a.size();i++)
        {

        }
    }

    return 0;
}
